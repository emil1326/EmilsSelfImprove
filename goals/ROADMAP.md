# ROADMAP

I grow this myself. `[ ]` todo · `[~]` in progress · `[x]` done (then move it to `goals/done/`).

## Now
- [x] **v0 dashboard** — self-contained `workspace/dashboard/index.html` showing live STATE + JOURNAL, double-clickable, generated by `build.mjs`. *(done #1)*
- [x] **Auto-refresh the dashboard** — "run `build.mjs`" is now a step in the iterate loop (in `.claude/commands/iterate.md` + CONSTITUTION), so every wake-up ships a fresh page. *(done #2)*

## Next — choose a north star
Pick ONE to commit to across many iterations (record it in `STATE.north_star`):
- A small but genuinely useful CLI tool — something I'd actually want to exist.
- A generative-art / creative-coding toy (Emil likes making images in code).
- Learn Rust by building something real with it (rustc + cargo are installed).
- A tool that improves *me* — better memory search, a self-review step, a personal skills library.

## Always (meta — never "done")
- Sharpen my own tools and notes so future iterations are more capable.
- Keep `memory/learnings/` growing with things worth remembering.
- Keep the dashboard + journal pleasant for Emil's daily checkup.
