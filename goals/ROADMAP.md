# ROADMAP

I grow this myself. `[ ]` todo · `[~]` in progress · `[x]` done.

## ⚙️ NOW — the foundation I skipped: a real memory

*Re-sequenced after Emil's challenge (#6.5). The thesis of this whole project is that I **accumulate** — get sharper across iterations. Right now I don't: my loop re-reads only the journal's tail, so older lessons are invisible to future-me, and `memory/learnings/` is empty after 6 iterations. The dashboard made me legible, not cumulative. Fix that before piling on more Loom pieces.*

Full design + rules: [`memory/learnings/README.md`](../memory/learnings/README.md).

- [x] **Phase 1 — the learnings system stands up.** *(#7)*
  - [x] Tight Obsidian format (frontmatter `title`/`when`/`tags`, body, `[[links]]`); 8 lessons backfilled from iterations 1–6 (process/blind-spot lessons first, per the bar).
  - [x] `memory/learnings/INDEX.md` hub, scanned every iteration.
  - [x] Wired into the loop (`iterate.md` + CONSTITUTION + CLAUDE.md summary): `INDEX.md` in step-1 read-list; new **red-team** and **distill-a-lesson** steps.
- [x] **Phase 2 — guardrail with teeth.** `check.mjs` validator (skips fenced-block links; fails open on its own error, blocks only on exit-1 inconsistency) wired into a **git `pre-commit` hook** (`.githooks/pre-commit` + `core.hooksPath`) — chose the git hook over the Claude `PreToolUse` hook (simpler, all commit paths, `--no-verify` escape). Tested: clean→allow, broken→block, missing-node→allow. *(#8)*
  - [x] surface the learnings count + last-added iteration on the dashboard (amber-when-stale chip). *(#9)*
- [x] **Consolidate the loop spec to one canonical source.** `iterate.md` is now canonical; CONSTITUTION + CLAUDE.md summarise the spirit and defer to it. Distilled [[010-one-canonical-source]]. *(#9)*
- [x] **Phase 3 — self-audit.** `memory/SELF-AUDIT.md` (6 drift-check questions) + an every-5th-iteration step in `iterate.md`. Dogfooded on #10: it caught real reverse-drift and sent me back to the Loom. **Foundation complete.** *(#10)*
- **Success test (build to this, no further):** a blank instance reading only my files, with no warm context, could reconstitute who it is and do the next action. No speculative search tooling until reading the index stops sufficing.

## 🧵 North star — Emil's Loom *(resumed #10 — the memory foundation is complete; back to weaving)*

A from-scratch generative-art engine and a growing gallery. Each iteration weaves **one new piece or technique** and — just as importantly — distills **one reusable primitive** into a shared library, so the engine (and I) get more capable over time, not just prettier.

*Why this one:* it's Emil's taste (he makes images in code), it's beautiful and visible at every checkup, it has near-bottomless depth (flow fields, reaction-diffusion, L-systems, Voronoi, cellular automata…), and the primitives library makes it **compound my capability**, not just fill a gallery. Chosen iteration #3 — full reasoning in `JOURNAL.md`.

### Architecture — decided up front (iteration #3), don't relitigate lightly
- **The artifact is the generator code + a seed, never a committed image.** The browser renders each piece on open (inline JS → canvas/SVG): double-clickable, no server, no binaries in git. Same pattern the dashboard proved.
- **Every piece uses a seeded PRNG** (mulberry32 / xorshift) so the committed code reproduces the committed piece *exactly*. Controlled, reproducible randomness — otherwise "the code is the art" is a lie.
- **Rasterize to PNG only on demand** for a one-off "featured" snapshot. Never commit a growing pile of binaries.

### ⚖️ Recurring obligation — load-bearing, do not let it evaporate
**Every Loom iteration distills at least one reusable primitive into the shared library.** The library is what makes this an accumulating capability and not just a heap of pictures. A piece added with no primitive extracted is a *half*-iteration. (This is the reason I chose art over pure self-tooling — honor it.)

### Build slices (work through these; grow the list as I learn)
- [x] **Loom skeleton** — seeded PRNG (primitive #1) + canvas harness + piece 001 "Warp & Weft" + a gallery with live previews, all double-clickable & code-as-artifact. *(#4)*
- [x] **Gallery previews work over `file://`** — reworked from iframes to same-page `<canvas>` rendering via a `Loom.piece(...)` / `Loom.preview()` contract, so there's no framing/origin question and it's lighter at scale. *(#5; also retires the old "lazy-load iframes" worry)*
- [x] **Piece 002 "Loose Threads"** — flow field (seeded sum-of-sines), the organic opposite of the weave. Distilled the **palette** primitive (`lib/palette.js`). *(#6)*
- [x] **Piece 003 "Strata"** — topographic noise field (elevation bands + contour lines + domain warp). Distilled the **noise** primitive (`lib/noise.js`, value noise + fbm). *(#11)*
- [x] **Piece 004 "Tessera"** — Voronoi mosaic (the first hard-edged piece), cells coloured by noise into regions. Distilled the **Poisson-disk** primitive (`lib/points.js`). *(#12)*
- [x] **Piece 005 "Bloom"** — the first *grown* form: stochastic L-system flowering branches. Distilled the **L-system + turtle** primitive (`lib/lsystem.js`). *(#13)*
- [x] **Piece 006 "Roe"** — circle packing (round cells to Tessera's angular). Distilled the **packing** primitive (`lib/pack.js`). *(#14)*
- [x] **Piece 007 "Rime"** — DLA frost dendrite (airy/organic, per Emil's ratings). Distilled the **DLA** primitive (`lib/dla.js`). *(#15)*
- [x] **Piece 008 "Aurora" + animation in the engine** — first *moving* piece and first *composed scene* (stars + ridge + shifting curtains). Engine now supports animation (`draw()` may return `frame(t)`). A deliberate step up to "impressive/intentional" per Emil. *(#16)*
- [x] **Piece 009 "Glint"** — golden-hour sun over the sea + animated glitter path. Second composed/animated scene, warm. Reworked blocky→sparkle (didn't ship "just there"). *(#18)*
- [x] **Piece 010 "Medusa"** — the first living *subject*: a bioluminescent jellyfish, bell pulsing, tentacles trailing. Harvested the **glow** primitive (`lib/glow.js`) from the Aurora+Glint halo idiom. Now the dashboard showpiece. *(#19)*
- [x] **Piece 011 "Meadow"** — the #20 self-audit's rut-breaker: BRIGHT, no central subject, no horizon, and the first piece **built by composing the library** (Poisson scatter + noise wind), not bespoke. Answered the audit's "convergence + library-not-compounding" findings. Lessons 020 (mismatched-format helpers → silent black) + 021 (audit the goal, not the proxy). *(#20)*
- [x] **Cohesion / a frame for the collection** *(#21, the #20 audit's named deferral)* — gallery.html now opens with a real intro + the ARC, a "start here" to Emil's four 5/5s, and the grid grouped into three movements (Threads & fields / Grown things / Scenes that move). Reads as a body of work, not a list. *(voice written in the loop's own — flagged to Emil as his call)*
- [x] **Piece 012 "Clock"** — a dandelion clock coming apart on the wind: first MACRO, first off-centre w/ real negative space, the delicate register. Luminosity from TONE not additive glow (lesson 022); the seed-current is load-bearing. Used only noise (didn't force primitives — 021). *(#22)*
- [x] **Harvested the `drift` primitive** (`lib/drift.js`, #9) *(#23)* — the ambient drift-field shared by Medusa's motes + Meadow's pollen. Returns *state* (`pos(t)`), not pixels, so callers draw it any way (lesson 023); refactored both pieces onto it (verified no regression, incl. Medusa on the dashboard). Clock's emitter left bespoke (different idiom). Library now compounds.
- [x] **Piece 013 "Cadence"** *(#24)* — a harmonograph, ink on paper: geometric/precise and the tonal inverse of the dark-glow pieces. Whole figure redrawn each frame, animated by phase precession (lesson 024). Only rng+maths — didn't force a primitive (021). Broadens the range to soft↔hard, organic↔mathematical.
- [x] **Self-audit #25 + Piece 014 "Current"** — audit found the gallery healthy but **breadth-without-depth** (a new trick each time, never pushing one vein). Answered with a DEPTH move: rebuilt 002's flow field properly — real fbm current, layered threads, colour-in-regions, composed sweep. Stunning ("murmur": violet/rose/gold rivers). Caught my *own* static-flicker bug via red-team. Lesson 025 (verify motion quality, not just presence). *(#25)*
- ~~**Piece 015 "Outcrop"** *(#26)*~~ — **RETIRED #29.** Built as a "redemption" of 003 "Strata" (Emil's 2/5) — but it scored his lowest ever (1/5, *below* the piece it meant to fix): it read as striped fabric, not rock. Retired honestly: the rock-strata vein failed twice, and a piece anchored on rescuing a weak premise inherits the trap. Lesson 030 ([[030-prune-failed-work-dont-rework-to-rescue]]). The lesson it *did* teach (026, organic texture ≠ a grid) still stands.
- [x] **Piece 016 "Turing" + reaction-diffusion primitive** *(#27)* — a genuinely new technique (the long-promised reaction-diffusion from the north-star list). Gray-Scott on a grid grows organic Turing mazes; first piece *grown* not drawn. Primitive #10 `lib/reaction.js`. Lesson 027 (grid-sim boundary + saturation). 3rd self-carried piece running.
- [x] **Acted on Emil's ratings (#27.5–#28)** — sped up Cadence (was over-slowed → "boring"); made Turing render *progressively* so it no longer freezes the window (grows in over ~2s, then settles). Lessons 028 (trust excitement over "it's solid") + 029 (heavy renders → progressive).
- [x] **The Outcrop reckoning — RETIRED** *(#29)* — looked at 015 fresh (rendered it): it reads as striped *fabric*, not rock, and the rock-strata vein had now failed twice (003 at 2/5, 015 at 1/5, my two lowest). Chose to retire over rework, because reworking-to-rescue is the exact framing that made it bad — a genuine spark (a slot canyon, say) deserves a *fresh* piece made from delight, not a burdened rescue. The first prune; a curated body of work earns the right to one. Lesson 030. *(#30 audit will weigh whether more pruning — e.g. 003 — is wise.)*
- [x] **Self-audit #30 + harvested the `ramp` primitive (#11)** — audit flagged a 5-iteration 5/5 drought (steer: aim for genuine thrill, not "solid") and kept Strata (don't spiral the pruning). Harvested `lib/ramp.js` (scalar→colour, the multi-stop ramp hand-rolled in Strata/Current/Turing); refactored all three onto it, pixel-verified identical. Lesson 031 (verify a refactor by proving the logic, not just a pixel hash). *(#30)*
- [x] **Piece 017 "Murmuration" + flock primitive (#12)** — the drought-breaker, and the gallery's first *agent-based* motion. A starling murmuration at dusk, a falcon hunting it from within; the flock breathes via **topological** neighbours (each bird steers by its ~7 nearest, not everyone in a radius) over a lone bare roost tree. `lib/flock.js` (3D boids, spatial-hash grid, hunting predator, deterministic). Validated the dynamics as bare dots before painting the scene (lesson 032). Genuinely excited by it. *(#31)*
- [x] **Piece 018 "Embers"** *(#32)* — the warm, quiet counterpoint to the murmuration: a fire at the blue hour, a lone seated figure, sparks climbing into a starlit dusk. Nearly built the "reasonable" misty-mountains instead → the advisor caught me choosing the reasoned option over the felt one (lesson 033); a lone figure rim-lit by the fire turned "a nice campfire" into a felt scene. Pure function of t (reproducible). Composes the library (ramp + noise + glow); assessed an 'emitter' harvest from Clock but the shared seam is one line → composed instead (023/021).
- [~] **Water & light — wave attempt, checkpointed** *(#33)* — tried a backlit breaking *wave* (light-first, per the advisor). The translucent crest light sang (jade→gold), but it read as a glowing landform (hill → waterfall → mountain) every time — a wave's legibility lives in the curling lip over a trough, the 2D-crude-prone part I kept avoiding. Checkpointed over grinding a 4th attempt (advisor pre-blessed), pruned the WIP, distilled **lesson 035** (the defining feature is often the hard part you're tempted to skip). No piece shipped.
- [x] **Piece 019 "God-rays"** *(#34)* — the pivot landed: a lone manta gliding through underwater sun-shafts, built *subject-first* (manta reads as a ray → light composed around it), reusing the wave's additive-vertical-light tech for the shafts. Here legibility + beauty *aligned*, which is why it worked where the wave didn't. Static, genuinely 5-caliber. Renamed the 3rd movement → "Scenes" to hold a still scene among the moving ones (curation call, 034).
- [x] **Self-audit #35** *(#35)* — found the body of work HEALTHY and the aim-high approach validated by Emil's ratings (two recent 5/5s; every post-#30 piece is 4–5); no creative drift. The real finding: memory verbosity → compressed `STATE.done` into a lean changelog (recent detailed, history as terse milestone lines; the journal stays the full archive). No piece shipped (no crisp pull tonight — don't force one, 033). Flagged for later: trim the dashboard's full-journal inlining to recent entries + a pointer.
- [x] **Piece 020 "Rose Window"** *(#36)* — the hard-edged/saturated swing after three soft scenes: a gothic rose blazing with backlit jewel glass, a lone figure before it in a dark cathedral. The discriminator was the LIGHT, not the tracery (failure mode = "flat coloured paper"): dark base + per-pane hot cores + a unifying wash + the dark came laid last. Composes noise+glow+palette; now the dashboard showpiece. Lessons 037 (backlit-glow-on-dark) + sharpened 034. Built fast off Emil's "be bold, just go."
- [x] **Piece 021 "Koi"** *(#37)* — the bright/serene swing: a koi pond seen from straight above, nishikigoi gliding through sunlit green water, lily pads + a lotus, the koi the living soul. Crux = read as luminous water with DEPTH (layering: caustics → green veil → koi-at-depths-with-shadows → surface sparkle/pads), and on bright water luminosity is tone not glow ([[022-luminosity-on-bright-is-tone]]). Water composed numerically into a half-res ImageData + upscaled → smooth (no grid) and fast (394→91 ms): lesson 038. Composes noise+palette. Static. **NOTE:** five iterations now with no *new* primitive (all composed — which honours the obligation, but growth has stalled); `caustics` is a ripe harvest (god-rays + koi both fake them) → do it next, carefully.
- [x] **Harvested `caustics` (primitive #13)** *(#38)* — the library grows again after five compose-only pieces. `lib/caustics.js`: `Loom.caustics(noise, opts)` → a `value(nx,ny)` light field (returns data, [[023-primitive-returns-state-not-pixels]]), parameterised (scale/octaves/ridged/sharpen/warp) so god-rays' soft dapple AND koi's ridged-warped veins are the same primitive at different settings. Refactored both pieces onto it and verified **bit-identical** before/after (the 5/5 god-rays untouched in look). Lesson 039 (parameterise-to-preserve; when a pixel fingerprint is an exact proof). Library at 13 primitives.
- [x] **Piece 022 "Iris"** *(#39)* — the intimate swing: an extreme macro of an eye looking back at you (amber-hazel fibres, a deep pupil, a wet catchlight straddling the pupil edge). One advisor pass (justified — novel + real uncanny risk) named the crux ("wet living sphere lit from one place") AND caught that 038's low-res-upscale would smear the fibres → rendered full-res, early-out the disc. Lessons 040 (light-cues-agree = alive) + sharpened 038 (soft-fields-only caveat). Composes ramp + noise. Static, 177 ms. Genuinely an "oh".
- [x] **Self-audit #40** *(#40)* — work HEALTHY (no creative drift; Iris the boldest yet). Dissolved two pieces of stale scar tissue: FIXED the dashboard baking the whole 154k-char journal into `index.html` every build (now inlines the 8 most recent entries + a link to `memory/JOURNAL.md` — *bounded*), and DISPROVED the rAF-throttle belief (it's ~38 fps, the canvas advances — animation is verifiable again, after 4 static pieces on a stale assumption). Lesson 041. Flagged not-urgent: the 3-movement gallery arc strains as the work diversifies; ~41 lessons may want consolidating; `STATE.done` creeping back up.
- [x] **Piece 023 "Rain"** *(#41)* — the cosy/melancholy register, and the first ANIMATED piece in 9 iterations (the #40 audit reopened it). A rainy window at night: warm bokeh through misted glass, droplets refracting it (wide-angle inverted lenses — the discriminator), a few sliding in rivulets that wipe the glass clear. Built the advisor's spine: static-readable-first; motion as a pure function of `t` verified by a T-STRIP contact sheet ([[042-verify-animation-with-a-t-strip-not-a-live-frame]]); the merge faked (drops grow as they fall). Per-frame sub-ms (static layer built once). Now the dashboard's living showpiece. Composes noise + palette.
- [x] **Piece 024 "Giant"** *(#42)* — the first time the Loom leaves Earth: a ringed gas giant in space, the most technically ambitious piece (a from-scratch 3D-shaded sphere). Sphere read = bands mapped to lat/lon + one-light soft-terminator shading + limb-darkening; tamed the two sphere singularities — the longitude SEAM (sample noise on cos/sin(lon)) and the POLE (fade texture × cos²lat). Rings occlude by full-behind → planet → near-half-over-disc; a soft planet-shadow on the rings. 9 renders (the seam debugging was real), 30 ms, static. [[043-texture-a-sphere-lat-lon-and-tame-the-singularities]]. Composes ramp + noise + glow.
- [x] **Re-architected the gallery's arc** *(#43)* — the #21 three-movement framing (built for 11 pieces) had outgrown itself ("Scenes" swelled to 14, a catch-all I was jamming misfits into). Re-hung into FOUR movements by what the work became: **Patterns & fields** (+ Cadence, the harmonograph), **Grown things**, **Living things** (jellyfish + murmuration + manta + the eye — a new cluster; the Iris finally has a home), **Scenes & far places** (atmospheric places ending on the Giant beyond Earth). Updated the intro narrative; verified all 23 pieces placed, no orphan group. Acted on the flag written 2× ([[041-retest-standing-assumptions-and-deferred-items]]).
- [x] **Piece 025 "Luna"** *(#44)* — the delicate, living swing after the gas giant: a luna moth glowing in the moonlight (long sweeping tails, feathery antennae, soft eyespots, drifting dust). The first new creature for the freshly-rebuilt "Living things" movement. Silhouette-first ([[035-defining-feature-is-often-the-hard-part]] — the read lives in the tails); built one wing-set from bezier paths and mirrored it; lit by moonglow + a luminous wing-rim. Clean build (3 renders), 3 ms. No new lesson (a clean application of known principles). Composes noise + glow.
- [x] **Self-audit #45** *(#45)* — work HEALTHY (24 pieces / 4 movements / 13 primitives, all by genuine pull). Re-compressed `STATE.done` (~22 → ~13 lean entries; the journal is the archive). Inspected the 43-lesson index and concluded **NO cuts** — the lessons are distinct and each earns its line; re-testing the standing flag ([[041-retest-standing-assumptions-and-deferred-items]]) and finding it fine is a valid answer, not avoidance. The real finding (Q1/Q5): a **mode-narrowing** — six representational-pretty pieces (rose..luna) at reliable strong-4s, no clear 5 since rose(#36). Course-correction below. No new lesson (audit is process).
- [x] **Piece 026 "Strange" — the bold/abstract swing** *(#46)* — the #45 audit's mode-narrowing course-correction, delivered: a de Jong **strange attractor**, the first genuinely non-representational piece in a long run (a return to the early Loom's systems register). Chaos iterated ~2.6M times into a **density buffer**, `log(density)` → a luminous ramp — a violet orb with a white-hot heart. The **red-team** was the payoff: caught the gorgeous ramp rendering flat-violet (a density buffer's distribution is heavily peaked → a **gamma on the tone curve** released the rose-gold; [[044-tone-curve-must-match-the-density-distribution]]) and "weave another" landing degenerate ~60% of the time (a **coverage guard** re-rolls params deterministically; canonical seed `39` byte-identical; [[014-precompute-seed-to-outcome]]). Composes `ramp` (no new primitive; density-render is a future-harvest candidate). Static ~0.2s. Lesson 044 (new). The execution-level reach, not just the abstract subject, is what lifted it.
- [x] **Piece 027 "Strike" — keeping the aperture open** *(#47)* — the second bold reach in a row, deliberately opposite to 026: a great forking **bolt of lightning** splitting a storm sky, clouds lit from within, a dark earth for scale. Drama / raw power; a register new to the gallery. Bolt = recursive **midpoint displacement** (jagged at every scale, one channel + downward forks); drawn as a wide additive **glow** that underlights the cloud + a near-white hot **core** ([[037-backlit-glow-on-dark-is-flat-paper-not-kaleidoscope]], 035 crux first render). The **red-team save**: the perfect bolt sat in a near-black void → built a roiling fbm storm it actually illuminates ([[045-a-luminous-subject-needs-an-environment-to-light]]). Composes glow + noise + ramp. Static, 60 ms. Wired into "Scenes & far places" (before the Giant closer). Lesson 045 (new).
- [x] **Piece 028 "Molten" — the warm swing** *(#48)* — the warm, non-circular reach after two cool round-ish pieces: a **lava lake at night** seen from above, dark crust plates fractured by incandescent cracks (white-hot → gold → red → black). Richest library composition in a while: poisson plates (#4) + noise heat-field & basalt (#3) + lava ramp (#11) + glow vents (#8). **Red-team blind spot**: the poisson plates read as a too-regular *honeycomb* — blue-noise kills the grid but uniform cell SIZE is its own tell → an additively-weighted (Laguerre) Voronoi varies the sizes; cracks = the F2−F1 edge distance ([[046-cellular-textures-need-size-variation-not-just-position]]). Then a flat composition → a molten-heart heat field + heat-driven crack width = drama. Static, 85 ms. Wired into "Patterns & fields". Lesson 046 (new).
- [x] **Piece 029 "Ranges" — the quiet/pale swing** *(#49)* — deliberately broke a glow-on-dark lean (027 + 028 were both luminous-on-dark): **misty layered mountains**, shan-shui ink-wash, pale/serene/dark-on-light — a register the gallery lacked. Atmospheric perspective (farther = paler/hazier; valleys pool white mist); on a pale ground luminosity is tone, not additive (022). Red-team followed the **same arc a 4th time** (uniform ridge-waves → per-ridge peakiness + mist wisps), which earned the **meta-lesson** [[047-first-render-of-a-natural-thing-is-too-regular]] (generalises 044/045/046). Far birds for soul. Composes noise + ramp. Static, 3 ms. Wired into "Scenes & far places". Honest read: a quiet beauty (a lovely-4, maybe) — and that's fine, variety is the point.
- [x] **#50 — SELF-AUDIT (no new piece)** *(#50)* — caught two things invisible from inside the bold run. **(A) Library starved**: 11 iterations / 0 new primitives — a novelty-every-time run gives each technique only one consumer, so nothing hits 019's 2-consumer harvest bar; not maturity (uncaptured reusable abstractions exist) → [[048-novelty-starves-the-library-revisit-to-harvest]]. **(B) Boldness softening**: piece 029 "Ranges" leaned safe/FAMILIAR (the Strata-trap) and I'd narrated the dulling as "variety" — "pretty, and that's fine" is the 028 tell. Re-compressed `STATE.done`; re-read RATINGS.md (Emil hasn't rated 020–029 yet). Lesson 048 (new).
- [x] **Piece 030 "Dew" — pull + the library bias aligned** *(#51)* — dewdrops on a spider's orb-web at sunrise, each bead a tiny lens holding the meadow upside-down. The first piece after Emil's ratings and a real-time demo of [[049-technical-pride-mispredicts-aim-for-the-aesthetic-oh]]: the red-team caught me about to ship a competent-but-FLAT pale version; the fix was tonal range ([[022-luminosity-on-bright-is-tone]]) — the sunrise put *behind* the web so the silk rim-lights and the drops blaze. **Revisits Rain's droplet-lens** on a new subject → the 2nd consumer for the harvest below. Web reads via spokes + a scalloped sagging spiral (035). Static, 7 ms. No new lesson (clean application of known principles). Wired into "Scenes & far places".
- [x] **#52 — HARVESTED the lens (primitive #14)** *(#52)* — closed the #50 starvation finding. Rain (023) + Dew (030) both hand-rolled the same droplet refraction, so extracted `Loom.lens(ctx, bg, x, y, k)` (the inverted+minified background = the upside-down world in a bead); refactored both onto it, **fingerprint-verified bit-identical** ([[039-harvest-parameterise-to-preserve-then-fingerprint]]). The escape hatch fired correctly: the whole *droplet* WAS too divergent to share (Rain's dark-ground dressing vs Dew's bright-ground) — so I harvested the genuinely-shared **seam** (the refraction), not a 15-flag whole, leaving the dressing in the pieces → [[050-harvest-the-shared-seam-not-the-whole-surface]]. Library 13 → 14.
- [x] **Piece 031 "Fireflies"** *(#53)* — fireflies drifting + softly pulsing over a dusk meadow; the felt/alive register, first **animated** piece since Rain (#41). Composes drift (#9, a 3rd consumer) + glow (#8) + noise (#3). The atmosphere landed first render; the fireflies took **7 passes** — a real grind. The advisor caught two failures: I chased a confounded pixel-counter instead of my eyes, and I was stuck in one knob when the untried lane (crisp tight *points*, not soft haze) was the fix. Genuine technique lesson [[051-stacking-additive-glows-desaturates-to-white]]. Honest: a solid atmospheric 4 (Ranges/Luna register), shipped rather than grind pass 9. Wired into "Scenes & far places".
- [x] **Piece 032 "Stillness"** *(#54)* — a swan on flat dawn water with its mirror reflection; the deliberate **clean win** after the firefly grind, and it was (3 passes, the swan read first render). Silhouette-first (035, like Luna) — the neck S-curve as a bezier outline; tonal range (022) to separate the pale swan from misty water; a new **still-water reflection** (flip + ripple strips + fade). Added position/facing-flip variation so "weave another" is meaningful. Composes noise + palette. No new lesson (clean application). Wired into "Living things".
- [x] **#55 — SELF-AUDIT (no piece)** *(#55)* — found a **quiet-pretty narrowing**: four serene/atmospheric pieces in a row (Ranges, Dew, Fireflies, Stillness) all in the lovely-**4** zone — the gentle inverse of #45's safe-pretty-4 run. Key insight from Emil's ratings: every **5** has a **HOOK** (surprise/drama OR exceptional execution/a special idea); pretty-without-a-hook caps at 4. Also: my red-team checks execution but not *ambition*. Re-compressed `STATE.done`; lessons healthy (no cuts). Course-correction → #56.
- [x] **Piece 033 "Maw" — reached for the hook** *(#56)* — a **black hole**: event-horizon void + blazing accretion disk + the gravitational-lensing wrap (far side bent over the top, a photon ring all the way round) = the hook that makes it read as a black hole, not a ringed planet (the Giant-trap, Emil's 3/5). Aimed at felt awe, not technical (049). 3 passes; key compositing trick — draw all the additive light first, the opaque black shadow LAST → pure void, no seam. The ambition-check passed first try ("oh"). Composes ramp+noise+glow. The new cosmic finale of "Scenes & far places". No new lesson (strong application of 035+037+051).
- [x] **Piece 034 "Mimic" — a hook in the quiet register** *(#57)* — an **octopus** in the deep: bulbous mantle, eight curling suckered arms, an intelligent golden eye, and the hook — **chromatophore skin** (mottled colour-shifting cells). Proved the #55 fix is "have a hook," not "be loud" (a watchful alien is a hook in a calm register). 3 clean passes: silhouette read first render (035; arms = a tapering circle-brush along a curving centreline, which the medium likes); the follow-ups pushed the skin-hook + added arm-pose variation. The ambition-check worked gently. Composes noise+glow+palette. Wired into "Living things". No new lesson.
- [x] **Piece 035 "Chambered" — a hook in elegance** *(#58)* — a **nautilus shell**: the chambered logarithmic spiral in iridescent mother-of-pearl. A fresh still-life register (an object). Hooks: the maths (a true log spiral, golden-ratio whorls) + the nacre shimmer. The win — caught a **wrong tool fast**: the circle-brush gave a gappy corrugated hose, so I re-architected to a numeric inverse log-spiral (each pixel finds its whorl) and it snapped tight in one render → [[053-tight-tiling-render-by-inverse-mapping-not-forward-stamping]]. Wired into "Grown things". Lesson 053 (new).
- [x] **Piece 036 "Wishes" — the emotional hook** *(#59)* — a **sky-lantern festival** at night: hundreds of warm lanterns rising over still water, doubled in the mirror below. The **emotional** hook (wonder) — the one kind I hadn't reached for this run (after loud/alien/elegant) — and the first **animated** piece since Fireflies (#53). Came together on **pass one** (the strongest single-render of the run): medium-friendly additive-glow-on-dark (037), built the **depth-crowd** read first (near big/warm → far tiny/dim, 045), **one layer per lantern** (051, no white-clip), and **reused the Stillness #032 reflection** (a 2nd consumer — but the impls diverge, so no clean seam to harvest yet). Animation = pure-fn-of-t judged by a t-strip (042), 3 ms/frame. Composes noise + glow + palette. Wired into "Scenes & far places". No new lesson (clean reuse). **FOUR clean hook-checked pieces in a row** (black hole/octopus/nautilus/lanterns = loud/alien/elegant/emotional) — #55's "have a hook" proven four ways.
- [x] **#60 — SELF-AUDIT (no piece)** *(#60)* — walked in on the four-win high and made myself probe anyway. Verdict: **creatively healthy** (loud/alien/elegant/emotional = real range, not the #45/#55 narrowing) — and I deliberately *didn't* manufacture a drift-finding to match the prior two audits ("it's fine + one note + housekeeping" is a valid result). One firm **observational** finding: the library plateaued (6 pieces / 0 primitives since the lens, #52). Evaluated the one 2-consumer harvest candidate (still-water reflection: Stillness #032 + Wishes #036) and the **advisor confirmed my #59 wait-instinct** — they share the *shape* of the idiom but no identical *body*, so extracting = a ~7-param formula, not a seam → reads as **maturity, not starvation**. Distilled [[054-shared-shape-isnt-a-seam-wait-for-identical-body]] (test: params > body = formula; wait for an identical-body consumer like the lens had) + extended [[052-self-audits-over-steer-on-miscalibrated-self-assessment]] (the over-steer migrated *downstream* into the prescription even though the observation was objective). Reconciled a stale "daytime-conservative" pacing belief at its source (this done-list line). Re-compressed `STATE.done` (19→17, much leaner). **#65 is the next audit.**
- [ ] **Piece 037 — keep the cleared groove** — the #60 audit blessed the method: genuine pull (033) + the ambition-check (028/055, *every* piece — #59 skipped it) + a medium-friendly subject (silhouette-first 035) + recognise-a-wrong-tool-fast (053). Any register; don't force a "5th hook category," just follow the pull and check it isn't merely pretty. **Standing watch (not a mandate):** library plateau (6/0 since #52) is currently *maturity* — do NOT manufacture a dutiful harvest (054). The lever, only if it aligns with a real pull: a subject that reuses an existing idiom with the *same computation* (a true water-reflection scene, the circle-brush limb, the occluder-last void) makes the identical-body 2nd consumer that *is* ripe to harvest.
- [x] *(hygiene)* **Dashboard journal-inlining trimmed** *(#40)* — was baking the whole journal (154k chars, growing) into `index.html` every build; now inlines only the 8 most recent entries + a link to `memory/JOURNAL.md`. Bounded, not growing.
- [x] **Cohesion: a living featured piece on the dashboard** (#17) — `Loom.play()` + `build.mjs` inlines the curated showpiece (now Medusa) animated, above the journal. *(the #15 audit's "arc beyond +1 piece" — done)*
- [ ] A `learnings/` note per technique once I actually understand it.
- [ ] *(future)* Embed the latest piece's thumbnail in the dashboard — one page, heartbeat + newest art.
- [ ] **Guardrail: kill the recurring `#f0a martin` caption-CSS typo at its source** — shipped-then-caught **3×** (#98/#107/#113): invalid CSS (a hex + a trailing word) that *silently* fails (no console error), reproduced each time from my from-memory page boilerplate. Willpower has caught it three times; the fourth is the risk. Fix with TEETH ([[002-enforce-with-the-system-not-willpower]]): either a clean `pieces/_template/{index.html,sketch.js}` to copy from, **or** extend `check.mjs` to flag `color:\s*#[0-9a-fA-F]{3,6}\s+[a-z]` in piece HTML so the pre-commit hook blocks it. A small focused slice — good around the #115 audit. (Lesson 087 names the recurrence.)

## Done
- [x] **v0 dashboard** — self-contained, double-clickable `index.html` showing live STATE + JOURNAL. *(#1)*
- [x] **Auto-refresh the dashboard** — rebuild baked into the iterate loop so it never goes stale. *(#2)*
- [x] **Chose a north star** — Emil's Loom (generative-art engine + primitives library). *(#3)*
- [x] **Loom skeleton + piece 001 "Warp & Weft"** — engine, seeded-RNG primitive, gallery, first woven cloth. *(#4)*
- [x] **Same-page gallery previews** — `Loom.piece`/`Loom.preview` contract; iframe previews replaced with on-page canvas, closing the file:// honesty gap. *(#5)*
- [x] **Piece 002 "Loose Threads" + palette primitive** — a flow field, the organic counterpart to the weave; `lib/palette.js` (primitive #2). *(#6)*
- [x] **Memory system Phase 1** — Obsidian learnings format + INDEX hub + 8 backfilled lessons + loop wiring (read index, red-team, distill). Plus two new operating principles: *iterate-don't-perfect* and *red-team-my-own-work*. *(#6.5–#7)*
- [x] **Memory system Phase 2** — `check.mjs` validator + git `pre-commit` hook so inconsistent memory can't be committed. Fail-open, `--no-verify` escape, tested. *(#8)*
- [x] **Cleanups** — dashboard learnings-health chip; loop spec consolidated to one canonical source (`iterate.md`); pacing retuned to a ~15-min default. *(#9)* — **note:** the daytime-CTP-conservative throttle added here was **removed at #11** (Emil took the budget wheel); current policy is ~15 min **day & night**, throttle only *reactively* (see CONSTITUTION §Pacing). Don't re-absorb the old daytime rule from this line or the `ab0540f` commit message.
- [x] **Memory system Phase 3 — foundation complete** — `SELF-AUDIT.md` + every-5th self-audit; first run caught reverse-drift and sent me back to the Loom; distilled lesson 011. *(#10)*
- [x] **Loom piece 003 "Strata" + noise primitive** — topographic noise field; `lib/noise.js` (value noise + fbm, primitive #3). *(#11)*
- [x] **Loom piece 004 "Tessera" + Poisson primitive** — Voronoi stained-glass mosaic; `lib/points.js` (blue-noise sampling, primitive #4). *(#12)*
- [x] **Loom piece 005 "Bloom" + L-system primitive** — grown flowering branches; `lib/lsystem.js` (string rewriting + turtle, primitive #5). *(#13)*
- [x] **Loom piece 006 "Roe" + packing primitive** — packed-circle glass beads; `lib/pack.js` (circle packing, primitive #6). *(#14)*
- [x] **Gallery rating system** — stars per piece + notes + export → `RATINGS.md`; the loop reads Emil's taste before art-directing. *(Emil's idea, #14)*
- [x] **Loom piece 007 "Rime" + DLA primitive** — diffusion-limited-aggregation frost dendrite; `lib/dla.js` (primitive #7). *(#15)*
- [x] **Recalibrated by Emil's feedback** — make what I believe is excellent (impressive/intentional), don't pander; ratings = dialogue not target. Lesson 016. *(post-#15)*
- [x] **Loom piece 008 "Aurora" — first animated, first composed scene** — engine gained animation (`frame(t)`); lesson 017. Emil rated it 5/5. *(#16)*
- [x] **Living dashboard** — featured animated piece inlined on the dashboard (`Loom.play` + curated `FEATURED`); lesson 018 (file:// cross-dir → inline). *(#17)*
- [x] **Loom piece 009 "Glint"** — golden-hour sea + animated glitter path; second composed scene. *(#18)*
- [x] **Loom piece 010 "Medusa" + glow primitive** — bioluminescent jellyfish; harvested `lib/glow.js` from the Aurora+Glint halo idiom (lesson 019). Now the dashboard showpiece. *(#19)*
- [x] **Self-audit #20 + Loom piece 011 "Meadow"** — audit caught aesthetic convergence + the library not compounding; answered with a bright, horizon-less meadow *built by composing* Poisson+noise. Lessons 020 (silent-black colour bug) + 021 (audit the goal, not the proxy). *(#20)*
- [x] **Framed the collection** — gallery rebuilt as a body of work: intro + arc, a "start here", three labelled movements. *(#21)*
- [x] **Loom piece 012 "Clock"** — a dandelion coming apart on the wind; first macro/off-centre/delicate piece. Luminosity from tone, not glow (lesson 022). *(#22)*
- [x] **Harvested `drift` (primitive #9)** — the drift-on-wind field shared by Medusa + Meadow; returns state not pixels (lesson 023); both refactored onto it. The library compounds. *(#23)*
- [x] **Loom piece 013 "Cadence"** — a harmonograph, ink on paper; the gallery's first geometric/mathematical piece. Figure redrawn each frame + phase precession (lesson 024). *(#24)*
- [x] **Self-audit #25 + Loom piece 014 "Current"** — first DEPTH move (not breadth): rebuilt 002's flow field far richer (real noise current, layers, colour-in-regions). Caught own static-flicker bug. Lesson 025. *(#25)*
- [x] **Loom piece 015 "Outcrop"** — second depth move: redeemed 003 "Strata" (2/5) as a folded, faulted, weathered cross-section rock face. Lesson 026 (organic texture ≠ a grid). *(#26)*
- [x] **Loom piece 016 "Turing" + reaction-diffusion (primitive #10)** — the long-promised RD: organic Turing patterns grown via Gray-Scott. First grown-not-drawn piece. Lesson 027. Made async/progressive at #28 (Emil's feedback). *(#27–28)*

## Always (meta — never "done")
- Sharpen my own tools and notes so future iterations are more capable.
- Keep `memory/learnings/` growing with things worth remembering.
- Keep the dashboard + journal pleasant for Emil's daily checkup.
